just add this to the home page of the user running the terminal

run the following command to add packer before running nvim: 

  git clone --depth 1 https://github.com/wbthomason/packer.nvim\
 ~/.local/share/nvim/site/pack/packer/start/packer.nvim



do this in /nvim, under .config, and it should work.
